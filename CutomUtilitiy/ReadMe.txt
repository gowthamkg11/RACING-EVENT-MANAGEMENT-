Using Automapper converts the Domain model to entity model
if once of the model implements IMapTo<User> converts to DBModel<User> 
if once of the model implements IMapFrom<User> converts to DomainModel<User> 
